# Binance API 凭据（可选）。
#
# 不配置也能用：行情、图表、本地记账都正常，只是持仓页看不到"交易所账户"那一块。
#
# 强烈建议只勾选【读取】权限：本项目只做 GET 查询，不需要交易/提现/划转权限。
# 若在 Binance 后台开启了 IP 白名单，记得把部署机器的出口 IP 加进去。
#
# 复制本文件为 backend/.env 后填写，该文件已被 .gitignore 忽略。
BINANCE_API_KEY=
BINANCE_API_SECRET=
